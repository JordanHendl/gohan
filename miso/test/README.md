This directory contains runnable sample applications that exercise Miso's rendering stack.

To try them out, enable the `miso-tests` feature and run the desired binary, for example:

```
cargo run --package miso --features miso-tests --bin hello_triangle
```
